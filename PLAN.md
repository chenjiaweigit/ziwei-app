# 紫微命盘 · 项目规划

## 当前状态

### 已上线页面

| 页面 | 路由 | 功能 | 入口 |
|------|------|------|------|
| 首页 | `/` | 品牌介绍、倪海夏体系说明、功能详解、名人命盘 | 直接访问 |
| 排盘 | `/chart` | 出生表单 → 命盘渲染 → AI 解读（流式输出） | 首页「立即起盘」 |
| 合盘 | `/heming` | 双人排盘 → AI 合盘分析 | 首页底部「合盘分析」 |
| 知识库 | `/knowledge/[star]/[topic]` | 14 主星 × 13 宫位 = 182 静态页面 | TabBar 古籍库入口在知识库内 |
| 古籍库 | `/library/[book]/[chapter]` | 紫微斗数全书/全集/骨髓赋 + 全文搜索 | TabBar 古籍库入口在知识库内 |
| 预览页 | `/preview` | 滚动开场动画 | 首页「了解倪海厦」 |
| 天纪 | `/tianji` | 6 子模块概览 + 24集DVD课程目录 + 倪师语录 | TabBar「天纪」 |
| 天纪详情 | `/tianji/[slug]` | 模块详情、章节、64卦可折叠网格、堪舆条目 | 天纪首页卡片点击 |
| 天纪课程 | `/tianji/episodes` | 24集DVD完整列表（展开要点） | 天纪首页入口 |
| 地纪 | `/diji` | 地纪模块列表 | TabBar「地纪」 |
| 人纪 | `/renji` | 人纪模块列表 | TabBar「人纪」 |
| 个人中心 | `/me` | 占位页（功能开发中） | TabBar「我的」 |
| 服务条款 | `/terms` | 静态条款 | 首页 footer |
| 隐私政策 | `/privacy` | 静态隐私 | 首页 footer |

### 用户系统

**位置：** `lib/db.ts` + `app/api/auth/*` + `lib/auth.ts`

- 手机号注册/登录（无密码，无验证码）
- JWT 认证（30 天有效期）
- JSON 文件数据库 (`data/db.json`)
- **无前端页面** — 没有登录/注册 UI，没有 `AuthProvider`

### 会员体系（待更新为小程序定价）

**当前**：只有月费/终身两级，支付 API 是模拟的。  
**目标**：重构为分层定价 — 广告层 → 单次付费 → 订阅 → 终身。

| 功能 | 免费（广告） | ¥9.9 单技 | ¥19.9 完整 | 月费 ¥19.9 | 年费 ¥98 | 终身 ¥99 |
|------|------------|-----------|-----------|-----------|---------|---------|
| 排盘 | 不限 | 不限 | 不限 | 不限 | 不限 | 不限 |
| AI 解盘 | 3次/天 | — | — | 不限 | 不限 | 不限 |
| 单宫位解读 | — | ✓ | — | ✓ | ✓ | ✓ |
| 完整详批 | — | — | ✓ | ✓ | ✓ | ✓ |
| 合盘 | — | — | — | ✓ | ✓ | ✓ |
| 流年推演 | — | — | — | ✓ | ✓ | ✓ |
| 去广告 | ❌ | ❌ | ❌ | ✓ | ✓ | ✓ |
| 云历史 | — | — | — | ✓ | ✓ | ✓ |
| 付费前端 | ❌ | ❌ | ❌ | ❌ 小程序内购 | ❌ 跳 H5 | ❌ 跳 H5 |

- ✅ **广告层**：免费用户 3 次/天，超限看激励视频解锁
- ✅ **单次付费**（¥9.9-¥39.9）：走微信虚拟支付，个人小程序可用
- ⏳ **订阅/终身**（¥19.9/月、¥98/年、¥99 终身）：跳转 H5 + LemonSqueezy 收款
- ❌ **当前支付 API 是模拟的**，需要重写

### API 路由

| 路由 | 方法 | 认证 | 说明 |
|------|------|------|------|
| `/api/generate` | POST | 否 | 排盘计算 |
| `/api/interpret` | POST | 可选 | AI 解盘 SSE（登录后限流） |
| `/api/heming` | POST | 否 | AI 合盘 |
| `/api/auth/register` | POST | 否 | 注册（手机号） |
| `/api/auth/login` | POST | 否 | 登录 |
| `/api/auth/me` | GET | 是 | 获取用户信息 |
| `/api/user/history` | GET/POST/DELETE | 是 | 排盘历史 CRUD |
| `/api/payment` | POST/GET | 是 | 支付/查询会员 |

---

## 🗺️ 定价方案 vs 现有功能对照表

**核心结论**：功能基本齐全，缺的是「付费门控逻辑」（判断用户是否付费、拦不拦）和「支付/广告接入」。

| 定价项 | 对应功能 | 现有代码位置 | 覆盖状态 |
|--------|---------|-------------|---------|
| **免费**（3次/天+广告解锁） | AI 解盘基础版 | `app/api/interpret/route.ts` 已有每日 3 次限流 | ⚠️ 缺广告解锁逻辑，缺未登录用户限流 |
| **¥9.9** 单宫位深度解读 | 5 个维度任选一个出 AI 报告 | `lib/ai/prompts.ts` 已有 `TOPIC_PROMPTS`（感情/事业/财运/健康/性格）<br>前端 `InsightPanel` 已有 6 个 tab 切换 | ✅ 功能完整<br>❌ **缺付费开关** |
| **¥19.9** 完整命盘详批 | 6 个维度全套一次出 | `lib/ai/interpret.ts` 已有 `generateInterpretation()` 函数 | ✅ 功能完整<br>❌ **缺付费开关** |
| **¥29.9** 双人合盘分析 | 双人 AI 合盘 | `app/api/heming/route.ts` 已有完整路由<br>`lib/ai/interpret.ts` 已有 `streamHeming()` | ✅ 功能完整<br>❌ **缺付费开关** |
| **¥39.9** 流年运势推演 | 未来一年逐月分析 | 前端 `TimeNav` 组件已支持 `大限/流年/流月` 视图<br>`chart/page.tsx` 已有 `view` 状态 | ⚠️ **缺 `annual` 主题 prompt**<br>❌ **缺付费开关** |
| **¥19.9/月** 月度会员 | 无限次 AI 解盘+合盘+去广告 | `app/api/payment/route.ts` 有模拟支付<br>数据库已有 `membershipTier` 字段 | ❌ **支付网关没接**<br>❌ 无自动续费 |
| **¥98/年** 年度会员 | 同月费，折 ¥8.2/月 | 同上 | ❌ 同上 |
| **¥99** 终身会员 | 永久全部权益 | 同上（数据库已有 `unlimited` 类型） | ❌ 同上 |
| **激励视频广告** | 看 30s 解锁 1 次 | 完全未开发 | ❌ 小程序上线后才能接 |

---

## 🗺️ 四阶段执行路线图

```
阶段一：合规文案    →  ✅ 已完成（Phase 1）
阶段二：小程序发布  →  ⏳ 当前执行（Phase 2）
   └─ ❌ 原生替换 → 暂不开发，先 WebView 上线看流量
阶段三：支付+广告  →  ❌ 暂不开发，等小程序跑起来再说
阶段四：原生优化    →  ❌ 暂不开发
```

### 阶段一：合规文案 ✅ 已完成

| # | 事项 | 涉及文件 | 完成日期 |
|---|------|----------|---------|
| 1 | AI Prompt 头部+尾部追加免责声明 | `lib/ai/prompts.ts` | ✅ |
| 2 | AI 流式输出末尾追加免责尾巴 | `lib/ai/interpret.ts` | ✅ |
| 3 | 服务条款补充付费会员章节（含定价+退款） | `app/terms/page.tsx` | ✅ |
| 4 | 隐私政策补充数据跨境说明 | `app/privacy/page.tsx` | ✅ |

---

### 阶段二：小程序上线 ⏳ **当前执行**

**思路**：用 `<web-view>` 直接加载现有 Web 页面，零代码改动上线验证流量。

#### 2.1 准备工作
- [ ] **注册微信小程序**，选择类目 **文化-传统文化**（不选占卜命理）
- [ ] **配置合法域名白名单**：把服务器域名加到小程序 request 白名单
- [ ] **创建小程序项目**，首页用 `<web-view>` 加载 `https://你的域名/chart`

#### 2.2 提审与验证
- [ ] **提交审核**
- [ ] **如审核失败**：退回纯 Web/H5 方案（部署到 Vercel/Cloudflare Pages）
- [ ] **如审核通过**：观察 UV、留存、用户反馈

#### ⛔ 阶段二中暂不开发的内容
> 以下内容不提前做，等 WebView 验证流量后再决定：
> - ❌ 将页面改为原生 WXML/WXSS
> - ❌ 接入微信登录
> - ❌ 替换排盘/付费页为原生组件

---

### 阶段三：定价体系与广告接入 ❌ **暂不开发**

> 等阶段二上线、流量跑起来后再启动。

#### 3.1 需要补的功能缺口
- [ ] **付费门控逻辑**：每个 AI 调用前检查用户是否付费/有无剩余次数
- [ ] **新增 `annual` 主题 prompt**：流年运势的 AI 提示词
- [ ] **激励视频广告**：小程序激励视频组件 + 后端解锁逻辑
- [ ] **未登录用户限流**：`/api/interpret` 和 `/api/heming` 对匿名用户加限流

#### 3.2 支付接入
- [ ] **小程序内单次付费**（¥9.9–¥39.9）：微信虚拟支付
- [ ] **跳转 H5 高价服务**（¥98–¥99）：LemonSqueezy + Webhook
- [ ] **重写 `app/api/payment/route.ts`**：对接真实支付网关
- [ ] **订单状态机**：`pending → paid / failed / refunded`
- [ ] **会员到期检查逻辑**：自动降级到免费

---

### 阶段四：原生小程序优化 ❌ **暂不开发**

> 以上全部跑通、流量稳定后，再考虑将高频页面改为原生组件以提升体验。

- ❌ 用 WXML/WXSS 重写首页
- ❌ 替换排盘页为原生组件
- ❌ 接入激励视频广告原生组件
- ❌ 开通流量主（UV ≥ 500 后自动开通）

---

### 定价心理依据（参考）

| 金额 | 心理价位 | 类比 | 预期转化率 |
|------|---------|------|-----------|
| ¥9.9 | 冲动消费 | 一杯奶茶 | 免费→付费 **8-15%** |
| ¥19.9 | 随便买 | 一份快餐 | ¥9.9→¥19.9 升级 **25-35%** |
| ¥29.9 | 略想一下 | 一杯星巴克 | |
| ¥39.9 | 考虑一下 | 一顿外卖 | |
| ¥98/年 | 感觉划算 | 每天 2 毛7 | 比月费省一半多 |
| ¥99 终身 | 一步到位 | 一顿火锅钱 | 老用户 **10-20%** 直接选 |

---

## 已完成的旧事项

### ✅ 导航优化
- TabBar 底部导航（5 tab / 首页/天纪/地纪/人纪/我的）、TopBar 40px、首页 4 功能入口卡片、Footer 链接修复

### ✅ 天纪模块详情页
- `/tianji/[slug]` 6 模块、易经 64 卦网格、堪舆核心概念卡片、`/tianji/episodes` 24 集 DVD 目录

### ✅ 视频源集成
- Bilibili（`BV1XrsHzcEPG` 83 页）+ YouTube（83 集高清重制），iframe 内嵌播放，不外链

---

## 📦 后续待开发（暂缓队列）

以下功能不属于当前四阶段主线，**现阶段不做**。等小程序上线、流量验证后，再根据用户反馈决定是否启动：

| 功能 | 涉及 | 原因 |
|------|------|------|
| `/renji/[slug]` 人纪模块详情页 | 内容型页面 | 对核心功能无影响，内容量大但 ROI 不明 |
| `/diji/[slug]` 地纪模块详情页 | 内容型页面 | 同上（数据量少，遗稿状态） |
| 全局 `AuthProvider` + 登录/注册 UI | 用户系统 | WebView 阶段无法使用微信登录，不做 |
| 会员权益展示页 `/membership` | 前端 UI | 支付未接入，做了也没用 |
| 用户中心（我的数据、历史记录、注销账号） | 前端 UI | 同上 |
| Cookie 同意弹窗 | 合规组件 | 小程序环境不适用，Web 退路时再加 |
| 知识库/古籍库首页列表页 | 导航优化 | 不影响核心排盘解读流程 |
| AI 输出水印/追溯 | 条款中提了但未实现 | 条款中删除对应内容即可 |
| 通用 API 速率限制中间件 | API 安全 | 流量上来之前不需要 |

---

## 🚨 合规红线 — 待逐一解决

### 一、平台载体选择

**现状**：项目是 Next.js Web 应用，未打包成任何小程序。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 1.1 | **小程序选策略** | 选 **文化-传统文化** 类目尝试提交，审核存在一定风险。失败则退回到 Web/H5 方案 | **P0** | — |
| 1.2 | **确认最终部署方式** | 列出可选方案并选型：①境内服务器+ICP备案（需公司主体或个人备案，国内访问快）②境外 Vercel/Cloudflare Pages（免备案，国内访问慢，无需主体）③公众号 H5 菜单跳转（需服务号+微信支付商户号） | **P0** | — |
| 1.3 | **域名备案状态确认** | 当前 `82.156.123.224:3000` 是 IP 直访，正式上线必须绑定已备案域名 | **P1** | — |

---

### 二、支付合规

**现状**：`app/api/payment/route.ts` 的 POST 方法直接调用 `db.payments.create()` + `db.users.update()` 标记会员升级，**没有任何真实的支付网关对接**。任何人都可以伪造请求直接升级会员。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 2.1 | **支付 API 是模拟的，不可用** | POST 方法直接写库标记已支付，无支付网关创建、无签名验证、无订单状态机 | **P0** | `app/api/payment/route.ts:10-38` |
| 2.2 | **个人不能直接收款** | 无支付牌照，必须通过 MoR 平台代收。候选方案：①**LemonSqueezy**（5%+$0.5/笔，个人可注册，代处理全球税务，支持信用卡+PayPal+Apple Pay）②**Paddle**（5%+$0.5/笔，类似）③**Stripe**（2.9%+$0.3/笔，费率低但税务需自理） | **P0** | `app/api/payment/route.ts` |
| 2.3 | **集成 MoR 平台必须做的工作** | ①注册平台账号 ②替换 POST `/api/payment`：创建 Checkout Session，返回跳转 URL ③实现 Webhook 接收回调 ④Webhook handler 中写库升级会员 ⑤添加订单状态（pending → paid/failed/refunded）⑥处理退款场景 | **P0** | `app/api/payment/route.ts` |
| 2.4 | **国内用户收款方案** | MoR 平台主要面向海外（信用卡）。若要收国内 RMB：需注册公司→申请微信支付/支付宝商户→但还需 ICP 和命理类目管控问题 | **P1** | — |
| 2.5 | **退款与争议处理策略** | MoR 平台代处理争议，但商家的退款政策（退款条件、时限等）需在服务条款中明确声明 | **P1** | `app/terms/page.tsx` |
| 2.6 | **定价显示方式** | MoR 平台以美元/欧元计价。如面向中国用户标 ¥29.9，实际扣款需汇率换算并告知用户 | **P2** | — |

---

### 三、内容合规 — AI 命理输出

**现状**：`lib/ai/prompts.ts` 中的 System Prompt 没有免责声明，只写了「不说危言耸听的话」。AI 输出可能包含对用户命运、财运、婚姻的具体断言。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 3.1 | **AI 提示词追加免责声明** | 在 `SYSTEM_PROMPT` 和合盘 prompt 开头加入：「免责声明：本解读基于传统命理体系生成，仅供自我认识与娱乐参考，不构成医疗、投资、法律、心理咨询或人生重大决策建议。」 | **P0** | `lib/ai/prompts.ts:1-12` |
| 3.2 | **AI 输出内容边界控制** | 在 Prompt 中限制：①禁止输出绝对化断言（你一定会/你绝对不会）②禁止预测具体死亡时间、重大灾难③禁止替代医疗/心理咨询建议④输出末尾固定追加免责标语 | **P0** | `lib/ai/prompts.ts` |
| 3.3 | **内容安全审核** | 如果用户量增长，建议接入审核服务过滤 AI 输出中的违规内容（涉政、色情、暴力等），可选阿里云内容安全或第三方 API | **P2** | `lib/ai/interpret.ts` |
| 3.4 | **AI 输出水印/追溯** | 服务条款中声称有「指纹追溯水印」，但目前代码中并未实现。需决定是否真做；不做则需从条款中删除 | **P1** | `app/terms/page.tsx:38` |
| 3.5 | **用户反馈机制** | 隐私政策提到用户可以对解读「准/不准」打分反馈，但前端没有实现此功能。要么实现，要么从隐私政策中删除 | **P2** | `app/privacy/page.tsx:24` |

---

### 四、个人信息保护（《个人信息保护法》合规）

**现状**：数据库 `lib/db/schema.ts` 中 phone 字段无加密；birth_info 和 chart 以 JSON 明文存储；JWT 密钥有硬编码降级。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 4.1 | **手机号加密存储** | 当前 `phone` 字段在 PostgreSQL 中为明文。应改为：①注册/登录时用 bcrypt 或 argon2 哈希后存储（不可逆）②或 AES 加密存储（可解密用于展示）③查询时用哈希匹配而非明文比对 | **P0** | `lib/db/schema.ts:5` |
| 4.2 | **生辰信息保护** | 出生年月日时+性别属于强个人识别信息，`birth_info` 字段当前以 JSON 明文存储。建议加密存储，或至少在使用后及时清理 | **P1** | `lib/db/schema.ts:17` |
| 4.3 | **JWT 密钥硬编码降级** | `lib/auth.ts:3` 中 `JWT_SECRET` 有 fallback 为 `'dev-secret-change-in-production'`。生产环境必须设置 `JWT_SECRET` 环境变量，否则任何人都可伪造 JWT 令牌 | **P0** | `lib/auth.ts:3` |
| 4.4 | **隐私政策弹窗确认** | 目前没有 Cookie 同意弹窗和隐私政策弹窗。需：①首次访问弹窗展示隐私政策要点+"同意"按钮②注册时单独勾选同意隐私政策和服务条款 | **P0** | 新建组件 |
| 4.5 | **数据跨境传输** | 数据库在 CockroachDB（AWS 新加坡/美国），AI API 调用 OpenRouter/NVIDIA（境外服务）。需：①在隐私政策中明确告知数据出境情况②依法获得用户单独同意③评估接收方数据保护能力 | **P1** | `app/privacy/page.tsx` |
| 4.6 | **用户数据可删除/可导出** | 隐私政策第5节承诺查询、删除、导出数据，但目前：①无前端 UI 实现"我的数据"页面②无数据导出 API③无账号注销 API。需补全实现或从隐私政策中删除未实现的功能 | **P1** | `app/privacy/page.tsx:49-54` |
| 4.7 | **数据留存期限策略** | 隐私政策未说明用户数据保存多久。建议明确：账户注销后多少天内删除所有数据，未登录访客的命盘数据是否保留/多久清理 | **P2** | `app/privacy/page.tsx` |
| 4.8 | **未成年人保护** | 隐私政策声明"面向 18 岁以上用户"，但注册/使用流程无任何年龄验证。建议注册时加入年龄确认勾选 | **P1** | `app/privacy/page.tsx:60` |

---

### 五、服务条款合规

**现状**：`app/terms/page.tsx` 已有初步条款，但部分内容与代码现状不一致，且缺少重要章节。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 5.1 | **服务条款需补充付费会员章节** | 当前无任何会员服务条款。需明确：①各档会员的权益范围②付费方式（通过指定 MoR 平台）③自动续费规则④退款政策⑤服务中断/终止对已付费用户的处理 | **P0** | `app/terms/page.tsx` |
| 5.2 | **无退款政策** | 法律要求（特别是欧盟/英国消费者合同法）提供 14 天退款期。MoR 平台有退款 API，但需制定具体规则并在条款中写明 | **P1** | `app/terms/page.tsx` |
| 5.3 | **服务中断补偿条款** | 如因服务器故障、AI 服务不可用等导致付费用户无法使用，条款中应说明是否有补偿 | **P2** | `app/terms/page.tsx` |
| 5.4 | **AI 免责声明加固** | 当前条款第4节有免责声明，但未专门针对 AI 生成内容局限性做说明。建议增加："AI 解读由大语言模型自动生成，可能包含不准确内容，平台不对其准确性做任何保证" | **P1** | `app/terms/page.tsx:41-43` |

---

### 六、安全 — 环境变量与密钥

**现状**：`.env.local` 中包含真实的 API Key，且有提交到仓库的风险。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 6.1 | **.env.local 存在真实密钥** | 文件包含 OpenRouter、NVIDIA 的 API Key 及数据库连接串（含密码）。确认 `.gitignore` 中包含 `.env.local`，生产环境使用环境变量管理 | **P0** | `.env.local` |
| 6.2 | **数据库密码强度** | CockroachDB 连接串中包含明文密码 `2q-vrVQdcVWWGfmUuYQJyw`，确保定期轮换 | **P1** | `.env.local` |
| 6.3 | **API Key 轮换计划** | AI API Key（OpenRouter、NVIDIA）到期/轮换策略 | **P2** | — |

---

### 七、倪海夏内容版权

**现状**：项目大量使用倪海夏《天纪》体系内容（14主星解读、课程架构、语录等）。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 7.1 | **确认内容来源性质** | 需明确：①知识库内容是学习笔记/个人总结（低风险）②还是直接搬运原文（高风险）③视频引用的是公开免费资源（已标注不可作为付费内容） | **P0** | — |
| 7.2 | **版权声明与合理使用** | 如是整理/总结类内容，建议添加声明：「本平台倪海夏相关内容仅供学习研究，版权归原作者/版权方所有」 | **P1** | 站点 footer |
| 7.3 | **避开商标/肖像侵权** | 避免直接使用「倪海夏」姓名作为品牌名或误导用户以为这是官方授权平台 | **P1** | 全站 |

---

### 八、API 安全与限流

**现状**：多个 API 端点缺少认证或限流。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 8.1 | **合盘 API 无任何限制** | `POST /api/heming` 完全开放，无认证、无限流、无会员检查。正式上线后至少应添加速率限制（如每 IP 每小时 N 次） | **P1** | `app/api/heming/route.ts` |
| 8.2 | **排盘 API 无速率限制** | `POST /api/generate` 无认证、无限流，理论上可被滥用 | **P1** | `app/api/generate/route.ts` |
| 8.3 | **未登录用户无限流** | `POST /api/interpret` 中限流仅对登录用户生效（每天3次），未登录用户可以不限次调用 | **P1** | `app/api/interpret/route.ts:22-37` |
| 8.4 | **添加通用速率限制中间件** | 建议在中间件层实现 IP + 路由级别的限流（可选 Upstash 或内存 Map） | **P2** | 新建文件 |

---

### 九、前端 UI 缺失项

**现状**：PLAN.md 已记录了多个未实现的 UI，此处汇总对齐。

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 9.1 | **无登录/注册页面** | Auth API 已实现，但无前端登录/注册界面 | **P1** | — |
| 9.2 | **无会员购买页面** | 无 `/membership` 页面展示会员价格和权益对比 | **P1** | — |
| 9.3 | **无用户中心/个人数据管理** | 无"我的数据""我的历史""注销账号"页面 | **P1** | — |
| 9.4 | **无 Cookie 同意弹窗** | 隐私政策第6节提到使用 Cookie/localStorage，但未征求用户同意 | **P0** | 新建组件 |
| 9.5 | **无年龄确认弹窗** | 隐私政策声明 18+，但无任何确认 | **P1** | — |

---

### 十、其他

| # | 事项 | 详情 | 优先级 | 涉及文件 |
|---|------|------|--------|----------|
| 10.1 | **添加 NOTICE.md 或备案信息页** | 境内部署需在网站底部展示 ICP 备案号+公安备案号 | **P1** | 新建 |
| 10.2 | **无障碍与可访问性** | 如面向国内公共服务标准，可考虑适老化/无障碍（非强制） | **P3** | — |
| 10.3 | **定期审计复查** | 以上所有项完成后，建立半年一次的合规复查流程 | **P3** | — |
